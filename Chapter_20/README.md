# Chapter 20 — Searching and Sorting

Exercises implementing classic sorting algorithms.

| Exercise | What it demonstrates |
|---|---|
| `exercise_20.merge_sort` | A recursive merge-sort implementation on a `std::vector<int>`, split across `main.cpp`, `merge_sort.cpp`, and `merge_sort.hpp`. |
